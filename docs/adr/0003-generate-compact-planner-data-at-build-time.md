# 0003: Generate Compact Planner Data At Build Time

Status: Accepted

The frontend loads generated planner data, not the raw Satisfactory `en-US.json`. The extractor CLI reads raw docs offline and writes stable, compact JSON for the app.
