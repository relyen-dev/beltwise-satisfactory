export type { GameDataset, ItemId, MachineId, RecipeId } from '@beltwise/game-data';

export interface RatePerMinute {
  amountPerMinute: number;
}

export interface Point {
  x: number;
  y: number;
}

export interface Size {
  width: number;
  height: number;
}
